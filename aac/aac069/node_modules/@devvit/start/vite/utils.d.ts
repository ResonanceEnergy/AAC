import type { AppConfig } from '@devvit/shared-types/schemas/config-file.v1.js';
import type { RollupLog } from 'rollup';
export declare const shouldSuppressWarning: (warning: RollupLog) => boolean;
export declare const shouldCheckClientForServerImports: (environmentName: string | undefined) => boolean;
/**
 * Reads and parses the devvit.json configuration file.
 */
export declare function readDevvitConfig(repoRoot: string): AppConfig;
/**
 * Extracts client entry points from devvit.json config.
 */
export declare function resolveRepoRoot(configRoot?: string): string;
export declare function resolveClientRoot(repoRoot: string): string;
/**
 * We allow the publicDir to be in the repo root or the client root. The client
 * root is to support all legacy devvit web apps out there (or just personal)
 * preference.
 */
export declare function resolvePublicDir(repoRoot: string, clientRoot: string): string | undefined;
export declare function getClientInputs(config: AppConfig, repoRoot: string, clientRoot: string): Record<string, string> | undefined;
/**
 * Resolves the server entry point.
 *
 * Checks for src/api/index.ts first, then falls back to src/server/index.ts
 * for backwards compatibility.
 */
export declare function getServerEntry(repoRoot: string): string;
//# sourceMappingURL=utils.d.ts.map