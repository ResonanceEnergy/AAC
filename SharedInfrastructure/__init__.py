"""
SharedInfrastructure — backward-compatible re-export package.

Canonical implementations live in ``shared/``. This package provides
import aliases so legacy code using ``from SharedInfrastructure.xxx import …``
continues to work.
"""
